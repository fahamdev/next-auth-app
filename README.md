This is a Next.js project which uses next-auth to handle authentication.

## Getting Started

First, run the backend dummy server:

```bash
yarn start:server
```

Then, run the Next.js app:

```bash
yarn dev
```
